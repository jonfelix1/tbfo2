# Tubes TBFO2
# import tokenizer
import gistfile1
import cyk_parser
import grammar_converter

print('Initiating...')

filename = str(input('Masukan filename (dengan .txt) : '))

input = open(filename, 'r').read()

print(input[6])